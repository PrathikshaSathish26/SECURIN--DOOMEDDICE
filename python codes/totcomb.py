# The faces Of the dice are defined
die_faces = [1, 2, 3, 4, 5, 6]

# Calculate the total combinations
total_combinations = len(die_faces) * len(die_faces)

# Print the result
print("Total combinations possible:", total_combinations)
